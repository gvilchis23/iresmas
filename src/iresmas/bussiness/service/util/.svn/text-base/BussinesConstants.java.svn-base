/**
 * 
 */
package iresmas.bussiness.service.util;

/**
 * @author Cristopher Cerrillo Callejas
 *
 */
public interface BussinesConstants {
	
	/** Algoritmo usado para encriptar los passwords */
   static final String ENC_ALGORITHM = "SHA";
   /** The name of the configuration hashmap stored in application scope. */
   static final String CONFIG = "appConfig";
   /** File separator from System properties */
   static final String FILE_SEP = System.getProperty("file.separator");
   /**The name of the Administrator role, as specified in web.xml*/
   static final String ADMIN_ROLE = "admin";
   /**The name of the User role, as specified in web.xml*/
   static final String USER_ROLE = "user";
   /**The name of the user's role list, a request-scoped attribute
    * when adding/editing a user.*/
   static final String USER_ROLES = "userRoles";
   /**The name of the available roles list, a request-scoped attribute
    * when adding/editing a user.*/
   static final String AVAILABLE_ROLES = "availableRoles";
}
